before_build:
  - cmd: nuget restore HeaderArrayConverter\HeaderArrayConverter.sln

configuration: 
  - Release

image: 
  - Visual Studio 2017

platform: 
  - Any CPU

environment: nodejs_version: "6"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
